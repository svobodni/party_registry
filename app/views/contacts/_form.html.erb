<%= simple_form_for @contact do |f| %>
<%= f.error_notification %>
<%= f.input :contact_type, collection: Contact.types %>
<%= f.input :contact %>
<%= f.input :privacy, collection: Contact.privacies %>
<%= f.button :submit, value: "Uložit", class: 'btn btn-success' %>
<% end %>
